import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xffb68157);      // A shade of Twitter Blue
  static const Color primaryLight = Color(0xfffbebd3); // A lighter shade of Twitter Blue

  static const background = Color(0xFFF9FAFB);   // Light neutral
  static const surface = Color(0xFFFFFFFF);      // White card/dialog

  static const textPrimary = Color(0xFF1F2937);  // Gray 800
  static const textSecondary = Color(0xFF6B7280); // Gray 500

  static const border = Color(0xFFE5E7EB);       // Light border

  static const Color accent = Color(0xFF2ECC71);       // Emerald Green (example for an accent color)
  static const Color warning = Color(0xFFF1C40F);      // Sunflower Yellow (example for a warning color)
  static const Color error = Color(0xFFE74C3C);        // Alizarin Red (example for an error color)
  static const Color success = Color(0xFF27AE60);      // Nephritis Green (example for a success color)
  static const Color text = Color(0xFF333333);         // Dark Gray text
  static const Color textLight = Color(0xFF888888);    // Lighter Gray text

  // Themed Jar Hints
  static const wooden = Color(0xFFB0885A);
  static const glass = Color(0xFFBCE0FD);
  static const minimalist = Color(0xFFCED4DA);
  static const cosmic = Color(0xFF6D28D9);
}
